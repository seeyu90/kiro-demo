<% content_for :title, "戰情室 — 專案階段追蹤" %>

<header class="dashboard-header">
  <div class="header-top">
    <h1 class="dashboard-title">戰情室 <span>— 專案階段追蹤</span></h1>
    <button type="button" id="theme-toggle" class="theme-toggle" aria-label="切換至淺色模式">🌙 深色</button>
  </div>
</header>

<%= turbo_frame_tag "project-phase-tracking-content" do %>
  <p class="breadcrumb">
    <%= link_to "入口頁", "/", class: "back-link", data: { turbo_frame: "_top" } %>
    <span class="breadcrumb-sep">›</span>
    <span class="breadcrumb-current">專案階段追蹤</span>
  </p>

  <% if @error.present? %>
    <div class="error-message">
      <strong>錯誤：</strong><%= @error %>
    </div>
  <% else %>
    <%= render partial: "overview" %>
  <% end %>
<% end %>
